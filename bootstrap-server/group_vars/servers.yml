bootstrap_user: hoang
bootstrap_pubkey: "{{ lookup('file', '~/.ssh/id_ed25519.pub') }}"

ssh_port: 22

ufw_allowed_ports:
  - 22 # SSH
  - 80 # HTTP
  - 443 # HTTPS

fail2ban_bantime: 1h
fail2ban_findtime: 10m
fail2ban_maxretry: 5
