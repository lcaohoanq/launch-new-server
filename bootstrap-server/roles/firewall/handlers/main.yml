---
# Handlers for firewall role

- name: reload ufw
  community.general.ufw:
    state: reloaded
  become: true

- name: reload firewalld
  ansible.builtin.systemd:
    name: firewalld
    state: reloaded
  become: true
