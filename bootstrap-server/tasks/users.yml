- name: Ensure sudo is installed
  ansible.builtin.apt:
    name: sudo
    state: present
    update_cache: true

- name: Create bootstrap user with sudo
  ansible.builtin.user:
    name: "{{ bootstrap_user }}"
    groups: sudo
    append: true
    shell: /bin/bash
    create_home: true
